# name: GCP Docker Deployment

# on:
#   push:
#     branches:
#       - dev
#       - prod

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Set Environment Variables
#         run: |
#           if [[ "${{ github.ref }}" == "refs/heads/dev" ]]; then
#             echo "VM_IP=${{ secrets.DEV_VM_IP }}" >> $GITHUB_ENV
#           elif [[ "${{ github.ref }}" == "refs/heads/prod" ]]; then
#             echo "VM_IP=${{ secrets.PROD_VM_IP }}" >> $GITHUB_ENV
#           fi

#       - name: Add SSH Key
#         run: |
#           echo "${{ secrets.GCP_SSH_KEY }}" > key.pem
#           chmod 600 key.pem

#       - name: Deploy to VM via SSH
#         run: |
#           ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.VM_SSH_USER }}@$VM_IP << 'EOF'
#             cd /home/${{ secrets.VM_SSH_USER }}/app || git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git app && cd app
#             git fetch --all
#             git checkout ${{ github.ref_name }}
#             git pull origin ${{ github.ref_name }}
#             docker compose down
#             docker compose up -d --build
#           EOF
